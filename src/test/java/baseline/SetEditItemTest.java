package baseline;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class SetEditItemTest {

    @Test
    void handleMouseClick() {
        //create an if statement for mouseEvent
        //if mouseEvent.getClickCount is equal to 2
        //call a function to set the stage for editItem
        //call a function to save the new Text from
        //take the returned value from SaveToDoEdit and update the listView
    }

    @Test
    void setEditItemStage() {
        //then set the stage for the editItem fxml
        //create a parent root
        //attach scene graph to scene
        //display in window's title bar
        //attach scene to stage
        //display the stage
        //call the ProcessEditItem
    }
}